const assert = require('assert');
const lumberjackServer = require('../index.js');

describe('lumberjackServer', function () {
  it('has a test', function () {
    assert(false, 'lumberjackServer should have a test');
  });
});
